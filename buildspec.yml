version: 2.0
phases:
  install:
  runtime-version:
  node-js: 20
  Commands:
    - npm install -g @angular/cli@17
  Commands:
    - npm install
   build:
  Commands:
     -ng build -c production
  artifacts:
  base-directory: dest/my-angular-project
  files:
   - '**/*'
